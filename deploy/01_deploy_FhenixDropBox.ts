// This project deploys with `npm run deploy`, which runs scripts/deploy.ts.
// The old hardhat-deploy plugin entrypoint is intentionally kept as a stub so
// TypeScript validation does not require the unused hardhat-deploy package.
export {}
